[package]
name = "luxifer-driver-grbl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "GRBL/miniGRBL-Treiber für LuxiFer: JobPlan → G-Code. Kennt nur den Core."

[dependencies]
luxifer-core = { path = "../../core" }
