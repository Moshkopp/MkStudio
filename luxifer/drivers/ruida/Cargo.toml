[package]
name = "luxifer-driver-ruida"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Ruida-Treiber für LuxiFer: JobPlan → Ruida-Binärjob. Kennt nur den Core."

[dependencies]
luxifer-core = { path = "../../core" }
